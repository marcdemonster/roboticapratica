#include <iostream>

using namespace std;

int main(){
    int a,b,c,d,prod;

    cin >> a;
    cin >> b;
    cin >> c;
    cin >> d;

    prod = ((a*b) - (c*d));

    cout << "DIFERENCA = " << prod << endl;

    return 0;

}